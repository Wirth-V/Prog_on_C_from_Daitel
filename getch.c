#include <stdio.h>

int main()
{
	int ch1;
	 char ch2;

	ch1 = getchar();
        ch2 = getchar();
        printf("Char1 is (%c) is  %d in tabl\nChar2 is (%c) is  %d in tabl\n",ch1, ch1, ch2, ch2);

	return 0;
}

